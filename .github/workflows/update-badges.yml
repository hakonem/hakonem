name: Update badges

on:
  schedule:
    # Runs at 12 noon UTC
    - cron: "0 12 * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: pemtajo/badge-readme@2.3.0
        with:
          CREDLY_USER: emma-louise-hakonsen
